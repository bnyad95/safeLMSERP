<x-app-layout>
    <x-slot name="header"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="text-xl font-semibold text-gray-800">Archived Courses</h2><p class="text-sm text-gray-600">Inactive catalog records available for restoration.</p></div><a href="{{ route('course-records.index') }}" class="rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-semibold text-gray-700 hover:bg-gray-50">Back to Courses</a></div></x-slot>
    <div class="py-10"><div class="mx-auto max-w-6xl space-y-6 px-4 sm:px-6 lg:px-8">
        <form method="GET" action="{{ route('course-records.archived') }}" class="flex flex-col gap-3 rounded-lg border border-gray-200 bg-white p-4 shadow-sm sm:flex-row sm:items-end"><div class="flex-1"><label for="archived-course-search" class="block text-sm font-medium text-gray-700">Search archived courses</label><input id="archived-course-search" type="search" name="q" value="{{ $search }}" placeholder="Code, name, or department" class="mt-1 block w-full rounded-md border-gray-300 text-sm shadow-sm focus:border-blue-500 focus:ring-blue-500"></div><div class="flex gap-2"><a href="{{ route('course-records.archived') }}" class="rounded-md border border-gray-300 px-4 py-2 text-sm font-semibold text-gray-700 hover:bg-gray-50">Reset</a><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white hover:bg-gray-800">Search</button></div></form>
        <section class="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200"><thead class="bg-gray-50"><tr><th class="px-5 py-3 text-left text-xs font-medium uppercase text-gray-500">Course</th><th class="px-5 py-3 text-left text-xs font-medium uppercase text-gray-500">Organization</th><th class="px-5 py-3 text-left text-xs font-medium uppercase text-gray-500">Archived</th><th class="px-5 py-3 text-right text-xs font-medium uppercase text-gray-500">Action</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($courses as $course)<tr><td class="px-5 py-4"><p class="text-sm font-semibold text-gray-900">{{ $course->code }} - {{ $course->name }}</p><p class="mt-1 text-xs text-gray-500">{{ $course->credits }} credits</p></td><td class="px-5 py-4 text-sm text-gray-600">{{ $course->department->college->name ?? 'No college' }}<p class="mt-1 text-xs text-gray-500">{{ $course->department->name ?? 'No department' }}</p></td><td class="px-5 py-4 text-sm text-gray-600">{{ $course->deleted_at?->format('M j, Y') }}</td><td class="px-5 py-4 text-right"><form action="{{ route('course-records.restore', $course->id) }}" method="POST">@csrf @method('PATCH')<button class="rounded-md bg-blue-600 px-3 py-2 text-sm font-semibold text-white hover:bg-blue-700">Restore</button></form></td></tr>@empty<tr><td colspan="4" class="px-5 py-12 text-center text-sm text-gray-500">No archived courses found.</td></tr>@endforelse</tbody></table></div>@if($courses->hasPages())<div class="border-t border-gray-200 px-5 py-4">{{ $courses->links() }}</div>@endif</section>
    </div></div>
</x-app-layout>
